# Implementation Plans

Generated by the improve skill on 2026-07-27 against v2.17.1 (`9731fc3`), from the
structural-debt survey at `docs/reviews/2026-07-structural-debt-survey.md`. Execute in
the order below unless dependencies say otherwise. Each executor: read the plan fully
before starting, honor its STOP conditions, and update your row when done.

These plans are self-contained by design (an executor needs only the plan file and the
repo). They are also compatible with being staged as Elves batches — each plan's "Done
criteria" maps directly onto batch acceptance rows if the maintainer prefers that route.

The operator was away at selection time; per the improve skill's non-interactive
default, the top five findings by leverage (impact ÷ effort, security first) were
planned. Everything else is recorded in the survey's registry with fix sketches.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Close the redaction parity gap and unify the secret corpora | P1 | M | — | TODO |
| 002 | Make preflight's gh-identity extraction total | P1 | S | — | TODO |
| 003 | Make the verification signal honest on every host | P1 | S | — | TODO |
| 004 | Heredoc gate, then extract run_fugu.sh's Python to a module | P2 | M/L | 003 (soft) | TODO |
| 005 | Fail closed on tar extraction without `filter=` | P2 | S | — | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (one-line reason) | REJECTED
(one-line rationale).

## Dependency notes

- 004 is recommended after 003 because 003 gives sub-floor hosts a clean skip signal
  and puts a floor guard in the shim 004 preserves; 004's steps 1–2 (the heredoc
  compile gate) are independent and can land any time.
- 001 and 003 both touch test expectations lightly; execute serially, not in parallel,
  to avoid suite-count churn confusing the baselines each plan records.

## Next wave (surveyed, not yet planned)

In leverage order from the survey registry: C-03 (bounded/stdin-closed subprocess
sweep), C-04 (atomic `.elves-session.json` writes via `storage.atomic_write_json`),
C-05 (`run_grok.sh` wall budget + containment), D-06 (git-helper consolidation onto
`leases.run_git`), D-07/D-08 (pin-corpus broadcast form + checker registry), T-04/P-01/
P-02 (provider-test split, suite sharding, template-repo fixtures), DOC-02/03/05/06
(grammar/doc drift batch).

## Findings considered and rejected

- **full_run.py decomposition now** (D-10): deferred, not rejected — 0 commits since
  v2.12 means size is a comprehension cost, not a defect source; extract the streaming
  cluster when the file next has to change (TODO #92 stands).
- **Memoizing `check_repo_consistency` file reads**: measured at 0.31 s total; not
  worth code.
- **CI `setup-python` caching**: nothing to cache in a stdlib-only repo.
- **`test_dispatch_isolation.py:3400`'s 0.5 s assert**: fully mocked, ~100× margin —
  safe as-is.
- **`references/parallel-unittest-deferral.md` deletion**: maintainer call (DOC-08) —
  it is accurate but orphaned; link it or fold it, don't silently drop it.
