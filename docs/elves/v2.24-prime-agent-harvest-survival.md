# Survival guide: v2.24 prime-agent harvest

## Run Control

- **Run mode:** finite
- **User intent (verbatim, 2026-08-05):** "write a highly detailed plan to port everything out of
  prime-agent into elves that would improve the performance of elves" … "when done planning,
  stage teh work as an /elves run" — staging only; launch deferred to a later explicit step.
- **Stop policy:** staging session may stop once the staging commit is pushed; the launched run
  stops only per §Stop Gate, an explicit user stop, or a Hard Stop condition.
- **Actual stop conditions:** user says stop; Hard Stop (genuine blocker, unauthorized merge
  request, destructive non-negotiable, tip collision); Final Completion after M-A1–M-A6 with
  evidence + Final Readiness.
- **Checkpoint due by:** none set (no morning checkpoint or paid-compute deadline recorded).
- **Checkpoint semantics:** any future checkpoint is a delivery target, never a hard stop,
  unless the user states otherwise in a later controlling instruction.
- **May continue after checkpoint:** yes — finite run continues to Final Completion.
- **Final-response policy:** staging session may give its final response once staging is pushed.
  The launched run applies the Pre-Final Guard before any final response and never finalizes
  while the Stop Gate says no.
- **Batch completion rule:** every batch ends with exactly one acceptance-backed `Close` commit
  pushed, after ≥1 pushed progress slice, with acceptance rows + evidence in
  `.elves-session.json`.
- **Re-read rule:** after every host-owned commit and push, re-read this survival guide before
  doing anything else.
- **Checkpoint rule:** update Current Phase / Next Exact Batch / Active Compute in place at every
  checkpoint or wake; checkpoints never end the run.
- **Continuation rule:** the latest controlling user instruction wins and is copied into User
  intent verbatim; `continuation_guard` carries `stop_allowed` + `next_required_action`; after
  any compaction or wake, resume the single next required action immediately.
- **Plan:** `docs/plans/v2.24-prime-agent-harvest.md` (authoritative; 8 batches B0–B7, M-A1–M-A6)
- **Branch:** `feat/v2.24-prime-agent-harvest`
- **Worktree:** `/Users/ruthbrown-ennis/research/dev/elves-v2-24-prime-agent-harvest`
- **Branch tip at start / Collision tripwire START_TIP:**
  `5b374fdf8cdfe53f11b724fb6c58f7c858210484` (= `upstream/main`, v2.23.1)
- **Workspace ownership:** this run exclusively owns the worktree above and its branch. Other
  worktrees on this machine (`elves-audit-follow-ups`, `elves-parallelves`, the main checkout)
  are forbidden surfaces.
- **Remotes / push:** push remote is `origin` = `https://github.com/RBrownHOPE/elves` (fork).
  Push to `upstream` = `aigorahub/elves` is **denied (403)** under this machine's gh auth
  (`RBrownHOPE`). PR path: **cross-fork** into `aigorahub/elves` `main`. Merging the eventual PR
  requires aigorahub write access (not available in this session's auth).
- **Merge policy:** **not authorized** (`driver_authorized: false`). User-merges default.
  Landable cross-fork PR is the terminal deliverable.
- **Work driver:** trusted full-run, subscription-native worker. Deterministic route-worker
  recommendation recorded at staging:
  `provider=native transport=claude_code effort=high model=inherit_live_driver_model`
  (inputs: host=claude, execution-reasoning=high, review-risk=standard, provider=auto,
  prewalk=auto). Host-native in-session execution is the recorded fallback if the separate
  native worker lifecycle is unavailable.
- **Worker packet:** `docs/elves/worker-packet-v2.24-prime-agent-harvest.md`
- **Implementation lane:** native trusted full-run; no external fast/untrusted lane engaged.
- **Delegation scope:** the whole plan (B0 residuals through B7) to one persistent native worker
  in this worktree; driver retains staging, reconcile, cumulative review, readiness, PR, landing.
  No sub-delegation — workers never spawn workers.
- **Git mode:** `branch_progress` — worker commits/pushes only `feat/v2.24-prime-agent-harvest`
  to `origin`; protected refs, tags, PR operations, and merge stay host-owned.
- **Driver monitor mode:** parked; sanitized non-model `full-run-await` follow stream; wake only
  on `behavior_policy.py` conditions; every wait carries a fallback watchdog; heartbeat
  coalescing at the 900 s default.
- **Driver update policy:** no timed chat updates; user-facing updates only on material events
  (blocker, safety tripwire, checkpoint, terminal) plus the final report and notification.
- **Prewalk:** `auto` (reuse cached version/route-bound proof only; never spend on qualification)
- **Cobbler:** `default_for_session: true`
- **Monitor / follow:** parked driver + sanitized `full-run-await` stream; wake conditions per
  `behavior_policy.py`; no timed chat updates; every wait carries a watchdog.
- **Re-drive budget:** 3 (substantive failures only; transient = escalating backoff, no budget)
- **Review policy:** one cumulative terminal review, consolidated blockers, delta-only re-review,
  confidence-guided triage from `Confidence:` trailers.
- **Driver review policy:** driver stays parked during the full-run (no per-batch driver review on
  a healthy run); reconcile once at wake; the cumulative terminal review above is the driver's
  review surface; reviewer must emit a Confidence-Guided Review section.
- **High-risk checkpoints:** B6 Close (continuity watchdog — new operational surface: broad proof
  + authority-boundary review, per plan risk `high`); any `.elves-session.json` / calibration
  schema-surface change (checked against the additive-only Non-Negotiable before Close).
- **Continuation harness:** run memory files (this guide + `.elves-session.json` +
  `continuation_guard`) are the sole source of truth for continuation; host goal/continuation
  features are a seatbelt, never authoritative; every parked wait carries a fallback watchdog;
  wake conditions per `behavior_policy.py`, forbidden wake triggers respected.
- **E2E mode:** legacy two-call. **This session staged only; launch is a separate explicit step.**
- **Run docs:** this file; `docs/elves/execution-log-v2.24-prime-agent-harvest.md`;
  `docs/elves/learnings.md` (shared, read at orient); `.elves-session.json`.
- **Upstream idea source:** `PrimeIntellect-ai/prime-agent` @
  `c98941a2a5cf40faecf9b4648ac3c304abf48fd3` — ideas with attribution, **no vendored code**
  (plan Product decision 1).

## Stop Gate

- **Stop allowed right now:** yes — for the staging session only.
- **Why:** the user asked to stage, not launch. Staging is complete when the staging commit is
  pushed and preflight evidence is in the execution log. The run itself has not started; once
  launched, this gate flips to **no** until M-A1–M-A6 hold with evidence, Final Readiness passes
  at the exact tip, and a landable PR exists. Forbidden stop reasons apply from launch onward.
- **Planned batches remaining:** 8 of 8 (B0 residuals through B7).
- **Next required action:** launching session executes §Next action step 1.

## Next action (launch sequence, for the launching session)

1. Optional: switch gh auth if direct `aigorahub` push is preferred over cross-fork
   (`gh auth status` first; otherwise keep the recorded cross-fork path).
2. `cd /Users/ruthbrown-ennis/research/dev/elves-v2-24-prime-agent-harvest`
3. Re-run `./scripts/preflight.sh` (cheap re-verify), then re-read this guide and
   `.elves-session.json`.
4. Launch the trusted full-run per `references/e2e-chat-to-land.md` using
   `python3 scripts/cobbler_agents.py implement full-run-prepare … / full-run-launch …`
   (resolve exact grammar from `--help` / the reference — do not invent flags), pointing at the
   recorded worker packet. Park and follow via `full-run-await`.
   Fallback: host-native Core Loop batch-by-batch per `SKILL.md`.
5. First worker milestone: B0 completion (baseline `verify_repo.py --ci` green recorded in the
   execution log; scratch re-clone of prime-agent at the pinned SHA).

## Cobbler Session State

- **Cobbler default:** on — `default_for_session: true` (recorded in `.elves-session.json`).
- **Activated by:** staging (Cobbler-first is the default once a run is staged).
- **Scope:** run coordination and one-off questions (Quick Cobbler); no council lanes convened at
  staging.
- **Behavior:** classify → route → preserve dissent → fit one answer; read-only lanes;
  native-subagent-first; writes into run memory only when material.
- **Persistence:** durable via `.elves-session.json`; this section mirrors it.
- **Exit phrases:** "Cobbler Mode: off" (thread-local mode only; the session default stays on
  until the run ends or the user turns it off).

## Current Phase

- **Status:** staged — staging complete, worker not launched. Machine state: `staging` per
  canonical contract; next transition is `executing` at launch.
- **Active batch:** none (pre-launch). First active batch at launch: B0 residuals.
- **What was just finished:** staging — plan authored and committed, worktree/branch created on
  `upstream/main`, session synced + validated, worker packet written, preflight green
  (3 explainable warnings).
- **Single next action:** the launching session executes §Next action step 1 (re-run preflight,
  then launch the trusted full-run).

## Active Compute

- None. No worker session, no paid provider lanes, no background jobs. The staging session used
  only local git/python helpers. `caffeinate` observed running at preflight (host-owned, not this
  run's).

## Next Exact Batch

- **Batch:** B0 (residual rows), then B1 per plan order. No batches out of order; B4 depends on
  B1, B5 on B1–B4, B7 last.
- **Scope:** B0 residuals only — baseline `verify_repo.py --ci` green recorded in the execution
  log; worker re-clones prime-agent shallow and re-pins SHA
  `c98941a2a5cf40faecf9b4648ac3c304abf48fd3`; staged-session validation re-confirmed at launch.
- **Acceptance criteria:** B0-A1–B0-A3 as written in the plan (authoritative text in
  `docs/plans/v2.24-prime-agent-harvest.md`; synced ids in `.elves-session.json`).
- **Risk:** low for B0 (staging/baseline only); the first implementation batch B1 is `standard`.

## Effort Standard

Work as hard as you can for the full run on plan acceptance and blockers. Prefer deeper progress
on the planned path over the minimum acceptable change; when a batch closes early, take the
next highest-value action from the plan order rather than idling. Impact-path proof mid-run; broad
proof at terminal; deferred hygiene drained at terminal. Same drive on B7 as B1.

## Forbidden Stop Reasons

- Reaching a checkpoint (checkpoints are delivery targets, never stop boundaries)
- A commit landed or a push succeeded (including batch progress slices)
- Green CI or a PR existing
- User silence / "natural place to check in" / "this feels like a lot for one turn"
- Batch boundary reached; compaction occurred
- Advisory usage ceiling crossed (plan Product decision 3)

Only valid stops: user says stop; genuine Hard Stop condition; Final Completion after M-A1–M-A6
with evidence.

## Launch Readiness

- [x] Plan committed at `docs/plans/v2.24-prime-agent-harvest.md`; acceptance contract validate + sync green
- [x] Branch + dedicated worktree created on `upstream/main`; START_TIP recorded
- [x] Worker packet written and recorded (`worker_packet_path`)
- [x] Preflight run (3 explainable warnings recorded in execution log)
- [ ] Stop Gate initialized with `Stop allowed right now: no` — done by the **launching** session;
  the staging session deliberately records `yes` (staging-only, see §Stop Gate)

## Post-Checkpoint Control Loop

After any checkpoint or wake, in order:

1. Every completed batch must end with a commit and push (one acceptance-backed `Close`, ≥1 prior
   progress slice).
2. After every host-owned commit and push, re-read this survival guide before doing anything else
   — do not wait for user acknowledgment.
3. Ask: does the Stop Gate still say `Stop allowed right now: no`? If yes, continue the plan
   order immediately; update Current Phase / Next Exact Batch / Active Compute in place.
4. Checkpoints are delivery targets, never stop boundaries. Latest controlling user instruction
   wins and is copied here verbatim when it changes.

## After Any Compaction

Fixed read order, then resume the single next required action without questions:

1. This guide — Run Control section and Stop Gate first
2. `.elves-session.json` → `continuation_guard`
3. `docs/elves/learnings.md`
4. `docs/plans/v2.24-prime-agent-harvest.md` (authoritative acceptance)
5. `docs/elves/execution-log-v2.24-prime-agent-harvest.md`
6. `.ai-docs/manifest.md` if present → constitution if present

## Deferred hygiene

- (empty — populate during the run)

## Decisions made (staging session, 2026-08-05)

- **Base is `upstream/main` (5b374fd, v2.23.1), not `origin/main`:** `origin` is the RBrownHOPE
  fork, stale at a v2.17.1-era tip (`81eb1a1`). A first worktree mistakenly created on
  `origin/main` was removed and its unpushed empty branch deleted before any commit; recreated on
  `upstream/main`.
- Cross-fork PR path recorded after `git push --dry-run` proved 403 on upstream and success on
  origin.
- Plan file moved from repo root into canonical `docs/plans/v2.24-prime-agent-harvest.md`.
- Stale tracked v2.23 `.elves-session.json` (terminal, `stop_allowed: false`, foreign
  `worktree_path`) superseded on this branch by the new session file. The leak on `main` is noted
  as a Scout-mode cleanup candidate for this run (never rewrite `main` history for it).
- Route-worker inputs chosen: execution-reasoning=high (contract-heavy runtime work),
  review-risk=standard (B6 individually `high`; driver review covers it).
